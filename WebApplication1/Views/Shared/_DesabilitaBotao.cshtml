
<script>

    const botao = document.querySelector("[data-form-save]");

    botao.disabled = true;

    document.querySelector("[data-form-title]").addEventListener("input", function (event) {

        let titulo = document.querySelector("[data-form-title]").value;

        if (titulo.length == 0) {

            botao.disabled = true;
        } else {
            botao.disabled = false;
        }
    });

    //if (document.querySelector("textarea")) {

    //    document.querySelector("textarea").addEventListener("input", function (event) {
    //        // console.log("Deu Certo");
    //        let anotacao = document.querySelector("p").value;

    //        if (anotacao.length == 0) {

    //            botao.disabled = true;
    //        } else {
    //            botao.disabled = false;
    //        }
    //    });

    }
    
    

    

</script>